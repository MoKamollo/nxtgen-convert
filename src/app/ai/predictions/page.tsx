"use client";
import { AppLayout } from "@/components/layout/AppLayout";
import { Button } from "@/components/ui/Button";
import { EmptyState, ErrorState, FilterTabs, LoadingState, ModuleHeader, SearchField, StatGrid, StatusBadge } from "@/components/modules/ModulePrimitives";
import { apiUrl } from "@/lib/org";
import { AlertTriangle, ArrowRight, CircleDollarSign, Sparkles, Target, TrendingUp, UserRoundCheck } from "lucide-react";
import { useCallback, useEffect, useMemo, useState } from "react";

type Contact={id:string;firstName:string|null;lastName:string|null;email:string|null;score:number|null;status:string|null;daysSinceContact?:number|null;likelihood?:number};
type Deal={id:string;name:string;value:string|null;currency:string|null;stage:string|null;expectedCloseDate:string|null;forecastProbability:number};
type Data={churnRisks:Contact[];conversionCandidates:Contact[];closingSoon:Deal[];upsellOpportunities:Contact[];stageProbabilities:Record<string,number>};
const money=(value:string|null,currency="USD")=>new Intl.NumberFormat("en-US",{style:"currency",currency:currency||"USD",maximumFractionDigits:0}).format(Number(value||0));
export default function PredictionsPage(){
 const[data,setData]=useState<Data|null>(null),[loading,setLoading]=useState(true),[error,setError]=useState(""),[search,setSearch]=useState(""),[tab,setTab]=useState("all");
 const load=useCallback(async()=>{setLoading(true);setError("");try{const r=await fetch(apiUrl("/api/ai/predictions"));const j=await r.json();if(!r.ok)throw new Error(j.error);setData(j.data)}catch(e){setError(e instanceof Error?e.message:"Failed to load predictions")}finally{setLoading(false)}},[]);useEffect(()=>{load()},[load]);
 const sections=useMemo(()=>data?[{key:"churn",title:"Churn Risks",description:"Customers needing immediate contact",icon:AlertTriangle,rows:data.churnRisks,action:"Open contact",href:"/crm/contacts",tone:"red"},{key:"convert",title:"Likely to Convert",description:"Qualified leads with strong intent signals",icon:Target,rows:data.conversionCandidates,action:"Start deal",href:"/crm/deals",tone:"brand"},{key:"close",title:"Closing Soon",description:"Proposal and negotiation deals due within 30 days",icon:CircleDollarSign,rows:data.closingSoon,action:"Schedule follow-up",href:"/crm/calendar",tone:"green"},{key:"upsell",title:"Expansion Ready",description:"High-value customers without an open deal",icon:TrendingUp,rows:data.upsellOpportunities,action:"Create expansion deal",href:"/crm/deals",tone:"violet"}]:[],[data]);
 const visible=sections.filter(s=>tab==="all"||s.key===tab);
 return <AppLayout><div className="space-y-5 p-6"><ModuleHeader title="AI Predictions" description="Rules-based signals generated from your live CRM data" action={<Button variant="outline" onClick={load}>Refresh analysis</Button>}/>{error&&<ErrorState message={error} retry={load}/>} {loading||!data?<LoadingState/>:<><StatGrid stats={[{label:"Churn Risks",value:data.churnRisks.length,icon:AlertTriangle,tone:"red"},{label:"Conversion Candidates",value:data.conversionCandidates.length,icon:Target},{label:"Closing Soon",value:data.closingSoon.length,icon:CircleDollarSign,tone:"green"},{label:"Expansion Ready",value:data.upsellOpportunities.length,icon:UserRoundCheck,tone:"violet"}]}/><div className="flex items-center justify-between gap-3"><FilterTabs value={tab} onChange={setTab} options={[{value:"all",label:"All"},{value:"churn",label:"Churn"},{value:"convert",label:"Conversion"},{value:"close",label:"Closing"},{value:"upsell",label:"Upsell"}]}/><SearchField value={search} onChange={setSearch} placeholder="Search predictions..."/></div><div className="grid grid-cols-2 gap-4">{visible.map(section=>{const rows=section.rows.filter((row:any)=>`${row.firstName||""} ${row.lastName||""} ${row.name||""} ${row.email||""}`.toLowerCase().includes(search.toLowerCase()));const Icon=section.icon;return <section key={section.key} className="overflow-hidden rounded-xl border border-surface-800 bg-surface-900/50"><div className="flex items-center gap-3 border-b border-surface-800 px-4 py-3"><div className="flex h-9 w-9 items-center justify-center rounded-lg bg-brand-500/10 text-brand-400"><Icon size={17}/></div><div><h2 className="text-sm font-semibold text-surface-100">{section.title}</h2><p className="text-[11px] text-surface-500">{section.description}</p></div><span className="ml-auto rounded-full bg-surface-800 px-2 py-1 text-[10px] text-surface-400">{rows.length}</span></div>{rows.length===0?<div className="p-4"><EmptyState icon={Sparkles} title="No matching signals" description="The current data does not trigger this prediction."/></div>:<div className="divide-y divide-surface-800">{rows.slice(0,8).map((row:any)=><div key={row.id} className="flex items-center gap-3 px-4 py-3"><div className="min-w-0 flex-1"><p className="truncate text-xs font-semibold text-surface-100">{row.name||`${row.firstName||""} ${row.lastName||""}`.trim()||row.email}</p><div className="mt-1 flex items-center gap-2 text-[10px] text-surface-500">{row.stage&&<StatusBadge value={row.stage}/>} {row.score!==undefined&&<span>Score {row.score??0}</span>}{row.likelihood&&<span>{row.likelihood}% likelihood</span>}{row.daysSinceContact!==undefined&&<span>{row.daysSinceContact===null?"Never contacted":`${row.daysSinceContact} days inactive`}</span>}{row.value&&<span>{money(row.value,row.currency)}</span>}{row.forecastProbability!==undefined&&<span>{row.forecastProbability}% forecast</span>}</div></div><Button size="xs" variant="ghost" iconRight={ArrowRight} onClick={()=>{window.location.href=`${section.href}?selected=${row.contactId||row.id}`}}>{section.action}</Button></div>)}</div>}</section>})}</div></>}</div></AppLayout>;
}
